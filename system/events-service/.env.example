# Рабочий шаблон лежит в infra/compose/.env.example
# Скопируйте его в infra/compose/.env и отредактируйте значения:
#
#   cp infra/compose/.env.example infra/compose/.env
#
# Корневого .env для compose не требуется — используется infra/compose/.env (см. Makefile).
