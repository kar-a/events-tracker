# packages

Общие библиотеки без отдельного Docker-образа.

- **`contract/`** — схема события, npm-пакет в `contract/node/`, артефакты сборки — см. `contract/README.md`.

Подробнее: `docs/STRUCTURE.md` в корне `events-service`.
