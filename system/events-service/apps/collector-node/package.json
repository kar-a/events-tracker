{
  "name": "@trimiata/events-collector",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node --watch --enable-source-maps dist/server.js",
    "build": "tsc -p tsconfig.json",
    "start": "node --enable-source-maps dist/server.js",
    "lint:types": "tsc -p tsconfig.json --noEmit",
    "test": "node --enable-source-maps dist/server.js --help >/dev/null 2>&1 || true"
  },
  "dependencies": {
    "@trimiata/events-contract": "file:../../packages/contract/node",
    "fastify": "^4.28.1",
    "pino": "^9.6.0",
    "pino-pretty": "^13.1.1",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "typescript": "^5.8.3"
  }
}

